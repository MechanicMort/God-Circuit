using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.Windows;

public class BlankScript : MonoBehaviour
{
    public int[] input;
    public int[] testArray;
    // Start is called before the first frame update
    void Start()
    {
        findUnique();
        /*
    Return the only unique number from an array.
    All numbers in the input array are present twice, except for one.
 
    Example:
    In the array [ 1, 8, 4, 4, 6, 1, 8 ], all numbers are present twice except 6.
    You should return 6.
 
    Hint: There are functions available to do the following:
        Array.indexOf(String) will return the first index where String can be found in the array
            Example:
                Let array = ["H", "e", "l", "l", "o"]
                array.indexOf("l") = 2
        Array.lastIndexOf(String) will return the last index where String can be found in the array
            Example:
                Let array = ["H", "e", "l", "l", "o"]
                array.lastIndexOf("l") = 3


        for(int i = 0;array.length;i++){
        if(array.indexOf(arrat[i]) == array.lastIndexOf(array[i])){        
                 return(array.indexOf(array[i]))
        }
        }
    
        */

    }

    public void findUnique()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
